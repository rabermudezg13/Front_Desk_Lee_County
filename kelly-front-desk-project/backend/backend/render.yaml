services:
  - type: web
    name: kelly-analytics-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python run.py
    envVars:
      - key: PORT
        value: 10000
      - key: FIREBASE_CREDENTIALS
        sync: false